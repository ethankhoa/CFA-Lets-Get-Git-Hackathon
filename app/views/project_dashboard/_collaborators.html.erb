<div class="box">
    <div class="media-content">
        <div class="content">
            <h1 class="subtitle" style="padding:0;">
                COLLABORATORS
            </h1>
            <% @approved_collaborators.each do |c| %>
            <table class="table">
                <tr>
                    <th>Title</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th></th>
                    <!-- add button to change status of collaborators here -->
                </tr>

                <td><%= c.project.title %></td>
                <td><%= c.user.username %></td>
                <td><%= c.user.email %></td>
                <td>
                    <a class="button is-info is-outlined">Delete</a>
                </td>
            </table>
            <% end %>
        </div>
    </div>
</div>
